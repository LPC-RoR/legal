<div class="cllps">
	<b>Agregar Nota</b>
	<%= form_for :form_antecedente, url: "/age_actividades/#{objeto.id}/agrega_antecedente?c=#{controller_name}" do |form| %>

		<%= render partial: '0p/form_fields/radio', locals: { form: form, token: :tipo, radio_list: ['Información', 'Advertencia', 'Urgente'] } %>

		<%= render partial: '0p/form_fields/text', locals: { form: form, token: :nota } %>


		<button type="submit" class="btn btn-info">
		  <i class="bi bi-caret-right icon-white"></i>
		</button>

	<% end  %>
</div>