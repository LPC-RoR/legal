<%= form_with(model: age_log, local: true) do |form| %>
  <% if age_log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(age_log.errors.count, "error") %> prohibited this age_log from being saved:</h2>

      <ul>
      <% age_log.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :fecha %>
    <%= form.datetime_select :fecha %>
  </div>

  <div class="field">
    <%= form.label :age_actividad %>
    <%= form.text_field :age_actividad %>
  </div>

  <div class="field">
    <%= form.label :age_actividad_id %>
    <%= form.number_field :age_actividad_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
