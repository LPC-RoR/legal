<div class="ttl_lnks">
	<div class="ttl">
		<% if objt.estado == 'realizado' %>
			<i class="bi bi-check-lg text-primary"></i>
		<% else %>
			<i class="bi bi-circle-fill text-<%=objt.prioridad%>"></i> 
		<% end %>
		<span class="text-<%=objt.text_color %>">
			<%= dma_hm(objt.created_at) %>
		</span>
	</div>
	<div class="lnks">
		<% unless objt.estado == 'realizado' %>
			<%= render partial: 'actividades/age_pendientes/list/elems/set_prioridad', locals: {objeto: objt} %>
		<% end %>

		<%= link_to "/age_pendientes/#{objt.id}/realizado_pendiente", title: "#{objt.estado == 'pendiente' ? 'Marcar como realizada' : 'Marcar como pendiente' }", 'data-toggle' => 'tooltip', 'data-placement' => 'right' do %>
			<% if objt.estado == 'realizado' %>
				<i class="bi bi-arrow-left"></i> 
			<% else %>
				<i class="bi bi-check-lg"></i>
			<% end %>
		<% end %>

		<% unless objt.estado == 'realizado' %>
			<%= render partial: '0p/tbl/lnks_e', locals: {objeto: objt} %>
		<% end %>
	</div>
</div>