<% ['d-age_pendientes', 'w-age_pendientes', 's-age_pendientes', 'n-age_pendientes'].each do |tabla_id| %>
	<% @coleccion[tabla_id].each do |age_pendiente| %>
		<%= render partial: 'actividades/age_pendientes/list/objeto', locals: {objeto: age_pendiente} %>
	<% end %>
<% end %>