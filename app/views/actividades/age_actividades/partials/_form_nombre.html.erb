<% unless @usuario.blank? %>
<div class="p-1">
<%= form_for :form_nombre, url: "/age_usuarios/#{@usuario.id}/personaliza" do |form| %>

	<%= render partial: '0p/lbld_inpts/text_raw', locals: { form: form, token: :nombre, label: 'Nombre usuario' } %>

	<div class="text-end">
		<button type="submit" class="btn btn-primary">
		  <i class="bi bi-caret-right icon-white"></i>
		</button>
	</div>

<% end  %>
</div>
<% end %>