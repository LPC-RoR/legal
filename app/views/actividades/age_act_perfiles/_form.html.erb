<%= form_with(model: age_act_perfil, local: true) do |form| %>
  <% if age_act_perfil.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(age_act_perfil.errors.count, "error") %> prohibited this age_act_perfil from being saved:</h2>

      <ul>
      <% age_act_perfil.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :app_perfil_id %>
    <%= form.number_field :app_perfil_id %>
  </div>

  <div class="field">
    <%= form.label :age_actividad_id %>
    <%= form.number_field :age_actividad_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
