
<div class="my-1 app_lnk">
	<b>Agregar Nota</b>
	<%= form_for :form_nota, url: "/notas/agrega_nota?c=#{controller_name}&clss=#{clss}&oid=#{oid}" do |form| %>

		<div class="row gx-1">
			<div class="col-11">
				<%= render partial: '0p/lbld_inpts/date', locals: { form: form, token: :fecha_gestion, label: 'Fecha Gestion' } %>

				<%= render partial: '0p/lbld_inpts/check_box', locals: { form: form, token: :sin_fecha_gestion, label: 'Sin fecha de Gestión' } %>

				<%= render partial: '0p/lbld_inpts/text_raw', locals: { form: form, token: :nota, label: 'Nota' } %>
			</div>
			<div class="col-1 text-end">
				<button type="submit">
				  <i class="bi bi-caret-right icon-white"></i>
				</button>
			</div>
		</div>



	<% end  %>
</div>