<div class="row gx-1 px-1 app_lnk" style="font-size: 13px; line-height: 1.2;">
	<div class="col">
		<%= render partial: '0p/tbl/ordr', locals: {objeto: objeto} %>
		<i class="bi bi-dot"></i>
		<%= objeto.codigo %>
	</div>
	<div class="col text-end">
		<% if dog? %>
			<%= render partial: '0p/tbl/lnks_e', locals: { objeto: objeto} %>
		<% end %>
	</div>
</div>