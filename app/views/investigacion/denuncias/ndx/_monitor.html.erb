<%= render partial: '0capitan/general/app_ttl', locals: {c: 'denuncias', title: "Denuncias", link: nil, tooltip: nil } %>

<%#= form_with url: "/causas", method: :get, local: true do |form| %>
  <!--div class="f_search">
    <div class="lft">
      <%#= form.text_field :query %>
    </div>
    <div class="right" align='right'>
      <button type="submit" class="btn btn-dark">
        <i class="bi bi-search icon-white"></i>
      </button>
    </div>
  </div-->
<%# end %>

<div class="f_200pxlft_col">
  <div class="lft">
    <%= render partial: '0p/cmenu/cmenu', locals: { c: 'denuncias', tipo: tipo, estado: estado, path: path} %>
  </div>
  <div class="rght">
    <%= render partial: '0capitan/general/app_ttl', locals: {c: 'denuncias', title: "Denuncias #{estado.blank? ? 'sin estado' : cmenu_clss['denuncias'][:items][estado].downcase}", link: '/denuncias/new', tooltip: 'Agregar denuncia' } %>
    <%#= @cs_array.blank? %>
    
    <%# unless @srch %>
      <%= render partial: '0p/tabla/tabla', locals: {c: 'denuncias'} %>
    <%# else %>
      <%# @cs_array.each do |fnd| %>
        <%#= render partial: 'causas/list/objeto', locals: {objeto: fnd} %>
      <%# end %>
    <%# end %>
  </div>
</div>