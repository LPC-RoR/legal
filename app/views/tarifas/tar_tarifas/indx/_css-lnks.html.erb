<%= render layout: '0p/collapse/link', locals: {label: 'AsignaTarifa', objeto: nil, tooltip: 'Asigna tarifa'} do %>
	<small>
	<% if ownr.tar_tarifa.blank? %>
		<i class="bi bi-plus-circle"></i>
	<% else %>
		<i class="bi bi-pencil"></i>
	<% end %>
	</small>
<% end %>