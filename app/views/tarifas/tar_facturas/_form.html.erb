<%= form_with(model: tar_factura, local: true) do |form| %>
  <% if tar_factura.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tar_factura.errors.count, "error") %> prohibited this tar_factura from being saved:</h2>

      <ul>
      <% tar_factura.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :owner_class %>
    <%= form.text_field :owner_class %>
  </div>

  <div class="field">
    <%= form.label :owner_id %>
    <%= form.number_field :owner_id %>
  </div>

  <div class="field">
    <%= form.label :documento %>
    <%= form.number_field :documento %>
  </div>

  <div class="field">
    <%= form.label :estado %>
    <%= form.text_field :estado %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
