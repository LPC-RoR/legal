<p><small><b>REGISTRO DE FACTURA</b></br>Ingrese información en virtud de la factura emitida original.</small></p>
<%= form_for :set_documento, url: "/tar_facturas/#{objeto.id}/set_documento" do |form| %>

	<%= render partial: '0p/lbld_inpts/date', locals: {form: form, token: :fecha_factura, label: 'Fecha de facturación'} %>
	<small><small>Si desea ingresar/cambiar la fecha de la factura, ingresar aquí la fecha.</small></small></br>
	<!-- NUMERO Y CONCEPTO -->
	<div class='row'>
		<div class='col-md-2'>
			<%= render partial: '0p/lbld_inpts/text', locals: {form: form, token: :documento, label: 'N°'} %>
		</div>
		<div class='col-md-10'>
			<%= render partial: '0p/lbld_inpts/text', locals: {form: form, token: :concepto, label: 'Glosa Factura'} %>
		</div>
	</div>
	<button type="submit" class="btn btn-info">
	  <i class="bi bi-check-all"></i>
	  Facturar
	</button>

<% end  %>