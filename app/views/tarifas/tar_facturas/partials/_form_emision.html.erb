<p><small><b>REGISTRO DE FACTURA</b></br>Ingrese información en virtud de la factura emitida original.</small></p>
<%= form_for :set_documento, url: "/tar_facturas/#{objeto.id}/set_documento" do |form| %>

	<%= render partial: '0p/form_fields/label', locals: {form: form, token: :fecha_factura, label: 'Fecha de facturación'} %>
	<%= render partial: '0p/form_fields/date', locals: {form: form, token: :fecha_factura} %>
	<small><small>Si desea ingresar/cambiar la fecha de la factura, ingresar aquí la fecha.</small></small></br>
	<!-- NUMERO Y CONCEPTO -->
	<div class='row'>
		<div class='col-md-2'>
			<%= render partial: '0p/form_fields/label', locals: {form: form, token: :documento, label: 'N°'} %>
			<%= render partial: '0p/form_fields/text', locals: {form: form, token: :documento} %>
		</div>
		<div class='col-md-10'>
			<%= render partial: '0p/form_fields/label', locals: {form: form, token: :concepto, label: 'Glosa Factura'} %>
			<%= render partial: '0p/form_fields/text', locals: {form: form, token: :concepto} %>
		</div>
	</div>
	<button type="submit" class="btn btn-info">
	  <i class="bi bi-check-all"></i>
	  Facturar
	</button>

<% end  %>