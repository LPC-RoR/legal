<p><small><b>REGISTRO DE PAGO</b></br>Ingrese información en virtud de la cartola del banco.</small></p>
<%= form_for :set_pago, url: "/tar_facturas/#{objeto.id}/set_pago" do |form| %>
	<small>
		<%= render partial: '0p/form_fields/label', locals: {form: form, token: :fecha_pago, label: 'Fecha de pago' } %>
		<% if objeto.fecha_pago.blank? %>
			<%= form.date_select :fecha_pago , {order: [:day, :month, :year], prompt: { day: 'Día?', month: 'Mes?', year: 'Año?' }, start_year: Date.today.year + 10, end_year: Date.today.year - 100}, {required: false} %></br></br>
		<% else %>
			<label>
			  <%= form.check_box :fecha_del_dia %>
			  Usar fecha del día
			</label>
		<% end %>
		<%= render partial: '0p/form_fields/label', locals: {form: form, token: :detalle_pago, label: 'Detalle transacción' } %>
		<%= render partial: '0p/form_fields/text', locals: {form: form, token: :detalle_pago} %>

		<button type="submit" class="btn btn-info">
		  <i class="bi bi-coin"></i> Registrar pago
		</button>
	</small>
<% end  %>
