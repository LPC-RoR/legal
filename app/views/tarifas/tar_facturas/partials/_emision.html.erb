<!-- Facturación -->
<p>
	<% if objeto.estado == 'ingreso' %>
	<a data-toggle="collapse" href="#collapseEmitir" role="button" aria-expanded="false" aria-controls="collapseEmitir">
    	Factura
	</a>
	<% else %>
		Factura
	<% end %>: 
	<%= "#{objeto.documento.blank? ? '(no emitida)' : objeto.documento} | #{dma(objeto.fecha_factura)} | #{objeto.concepto}" %>
</p>
<% if objeto.estado == 'ingreso' %>
	<div class="collapse" id="collapseEmitir">
	  <div class="card card-body">
		<%= render partial: 'tarifas/tar_facturas/partials/form_emision', locals: {objeto: objeto} %>
	  </div>
	</div>
<% end %>
