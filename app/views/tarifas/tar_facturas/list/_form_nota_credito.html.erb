<%= form_for :tar_nota_credito, url: "/tar_facturas/#{objeto.id}/crea_nota_credito" do |form| %>
	<div>

		<%= render partial: '0p/lbld_inpts/number', locals: { form: form, token: :numero, label: 'Número' } %>
		<%= render partial: '0p/lbld_inpts/date', locals: { form: form, token: :fecha, label: 'Fecha' } %></br>
		<%= render partial: '0p/lbld_inpts/number', locals: { form: form, token: :monto, label: 'Monto' } %>

		<%= render partial: '0p/lbld_inpts/check_box', locals: { form: form, token: :monto_total, label: 'Monto total' } %></br>

		<button type="submit" class="btn btn-info">
		   <i class="bi bi-arrow-right-circle"></i>
		</button>
	</div>
<% end  %>