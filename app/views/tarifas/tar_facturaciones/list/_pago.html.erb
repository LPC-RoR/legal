<div class="row">
	<div class="col-6">
		<% unless objeto.owner_id.blank? or ['RegReporte'].include?(objeto.owner.class.name) or objeto.find_pago.blank? %>
			<%= objeto.find_pago.tar_pago %> 
			<% unless objeto.tar_cuota_id.blank? %>
				( <%= objeto.tar_cuota.orden %> )
			<% end %>
		<% end %>
	</div>
	<div class="col-6" align="right">
		<b>
		<%= s_uf(objeto.monto_uf) %>
		<i class="bi bi-arrow-left-right"></i>
		<%= s_pesos(objeto.monto_pesos) %>
		</b>
	</div>
</div>
