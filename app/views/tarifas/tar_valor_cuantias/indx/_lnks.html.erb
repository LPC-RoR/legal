<% if @resumen %>
	<%= render layout: '0p/collapse/link', locals: {label: 'ActMontos', objeto: nil, tooltip: 'Resumen de cuantía'} do %>
		<i class="bi bi-filetype-ai"></i>
	<% end %>
<% end %>



<%= render layout: '0p/tltp_lnk', locals: {lnk: "/causas/#{blngs.id}/cuantia_to_xlsx", tltp: 'Exportar a Excel'} do %>
	<i class="bi bi-download"></i>
<% end %>

<% unless @objeto.tar_calculos.any? %>

	<i class="bi bi-dot"></i>
	<%= render partial: '0p/tbl/lnk_new', locals: {source: 'demandantes', blngs: @objeto, gly: 'person-lines-fill'} %>

	<% unless @objeto.demandantes.empty? %>
		<%= render partial: '0p/tbl/lnk_new', locals: {source: 'tar_valor_cuantias', ownr: @objeto} %>
	<% end %>

<% end %>