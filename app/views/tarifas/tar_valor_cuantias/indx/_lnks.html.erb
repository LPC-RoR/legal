<%= render layout: '0p/layout/tooltip_link', locals: {link: "/causas/#{ownr.id}/cuantia_to_xlsx", tooltip: 'Exportar a Excel'} do %>
	<i class="bi bi-download"></i>
<% end %>
<i class="bi bi-dot"></i>
<% if @audiencia_preparatoria.blank? %>
	<%= render layout: '0p/collapse/link', locals: {label: 'AudienciaPreparatoria', objeto: nil, tooltip: 'Fecha AudienciaPreparatoria'} do %>
		<i class="bi bi-calendar-check"></i>
	<% end %>
<% end %>
<%= render layout: '0p/layout/tooltip_link', locals: {link: "/demandantes/new?cid=#{ownr.id}", tooltip: 'Agregar demandante'} do %>
	<%= render partial: '0p/general/gly', locals: {gly: 'person-lines-fill'} %>
<% end %>
<% unless @objeto.demandantes.empty? %>
	<i class="bi bi-dot"></i>
	<%= render partial: '0p/tabla/lnk_new', locals: {lnk: "/tar_valor_cuantias/new?class_name=#{ownr.class.name}&objeto_id=#{ownr.id}", tltp: 'Agrega item de cuantía' } %>
<% end %>