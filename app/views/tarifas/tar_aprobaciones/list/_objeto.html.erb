<small>
<div class="list-item" style='background-color:#FFFFFF; padding-left: 1em; padding-top: .5em; padding-bottom: .5em; margin-bottom: 1em;'>
	<%= objeto.cliente.razon_social %> | <%= link_to dma(objeto.fecha), objeto %></br>
	<small><small>
	<% objeto.tar_facturaciones.each do |facturacion| %>
		<%= facturacion.glosa %> | 
		<%= s_pesos(facturacion.monto_pesos) %></br>
	<% end %>
	</small></small>
	<small>
		<%= link_to 'facturar', "/tar_aprobaciones/#{objeto.id}/facturar" if objeto.tar_facturaciones.where(tar_factura_id: nil).any? %>
		<%= render partial: '0p/tabla/links_e', locals: {objeto: objeto} %>
	</small>
</div>
</small>