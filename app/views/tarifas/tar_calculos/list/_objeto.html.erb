<div class="ln_dtll">
	<div class="ln_lft">
		<b><%= objeto.glosa %></b>
		<%= render layout: '0p/layout/tooltip_link', locals: {link: "/tar_calculos/#{objeto.id}/crea_aprobacion", tooltip: 'Crea aprobación'} do %>
			<i class="bi bi-plus-circle"></i>
		<% end %>
	</div>
	<div class="ln_rght">
		<%= objeto.moneda %>
		<%= s_pesos(monto_pesos(objeto, objeto.owner, objeto.tar_pago)) %>
	</div>
</div>