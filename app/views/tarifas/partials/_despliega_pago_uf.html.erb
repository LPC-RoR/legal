<% if objeto.pago_generado(pago).blank? %>
	<% if pago.moneda == 'Pesos' %>
		<%if objeto.uf_calculo_pago(pago).blank? %>
			UF de cálculo no encontrada
		<% else %>
			<%= s_uf5( calcula2(pago.formula_tarifa, objeto, pago) / objeto.uf_calculo_pago(pago).valor ) %>
		<% end %>
	<% else %>
		<%= s_uf5( calcula2(pago.formula_tarifa, objeto, pago) ) %>
	<% end %>
<% else %>
	<%= s_uf5(objeto.pago_generado(pago).monto_uf) %>
<% end %>