<%= form_with(model: tar_convenio, local: true) do |form| %>
  <% if tar_convenio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tar_convenio.errors.count, "error") %> prohibited this tar_convenio from being saved:</h2>

      <ul>
      <% tar_convenio.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :fecha %>
    <%= form.datetime_select :fecha %>
  </div>

  <div class="field">
    <%= form.label :monto %>
    <%= form.text_field :monto %>
  </div>

  <div class="field">
    <%= form.label :estado %>
    <%= form.text_field :estado %>
  </div>

  <div class="field">
    <%= form.label :tar_factura_id %>
    <%= form.number_field :tar_factura_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
