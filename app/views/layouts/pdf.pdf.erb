<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Reporte PDF</title>
    <%= pdf_stylesheet("pdf") %>
    <!-- OR if using the initializer approach: -->
    <%#= wicked_pdf_stylesheet_link_tag "pdf" %>
    
    <style>
      /* Estilo minimalista para tablas PDF */
      table.pdf-minimal {
        width: 100%;
        border-collapse: collapse;
        font-size: 9px; /* Tamaño de fuente reducido */
        line-height: 1.2; /* Altura de línea compacta */
      }
      
      table.pdf-minimal td,
      table.pdf-minimal th {
        padding: 3px 5px; /* Padding reducido (vertical horizontal) */
        border: none; /* Elimina todos los bordes */
        height: 18px; /* Altura fija para las filas */
      }
      
      /* Opcional: fondo alternado para mejor legibilidad */
      table.pdf-minimal tr:nth-child(even) {
        background-color: #f8f8f8;
      }
      td.wide-column {
        width: 50%;
        word-wrap: break-word; /* Rompe palabras largas */
      }
    </style>
  </head>
  <body>
    <div class="text-center" id="pdf-header">
      <%#= image_tag pdf_asset_base64('tyc.png'), class: 'header-image' %>
      <%= image_tag pdf_asset_base64(@logo_url), class: 'header-image' %>
    </div>
    <%= yield %>

  </body>
</html>