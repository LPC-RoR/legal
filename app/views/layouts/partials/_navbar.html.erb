<% logo_path ||= "logo/logo_60.png" %>
<nav class="navbar navbar-expand-lg navbar-light bg-light px-3">
  <div class="container-fluid">

    <% if menu_for_current_context %>
      <%= link_to '#', data: { bs_toggle: 'offcanvas', bs_target: '#verticalMenu' } do %>
        <%= image_tag logo_path, alt: "Logo", class: 'img-fluid' %>
      <% end %>
    <% else %>
      <%= image_tag logo_path, alt: "Logo", class: 'img-fluid' %>
    <% end %>


    <!-- Botón hamburguesa (móviles) -->
    <button class="navbar-toggler" type="button"
            data-bs-toggle="collapse"
            data-bs-target="#navbarMenu"
            aria-controls="navbarMenu"
            aria-expanded="false"
            aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Menú (derecha) -->
    <div class="collapse navbar-collapse justify-content-end" id="navbarMenu">
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to "Inicio", root_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "Info", "/hlp_ayudas/bnvnd", target: '_blank', class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%#= link_to "Contacto", root_path, class: "nav-link" %>
        </li-->
        <% if usuario_signed_in? %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button"
               data-bs-toggle="dropdown" aria-expanded="false">
              <%= current_usuario.nombre? ? current_usuario.nombre : email_mask(current_usuario.email) %>
            </a>
            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
              <li><%= link_to "Perfil", edit_usuario_registration_path, class: "dropdown-item" %></li>
              <li><hr class="dropdown-divider"></li>
              <li><%= link_to "Cerrar sesión", destroy_usuario_session_path,data: {turbo_method: :delete} , class: "dropdown-item" %></li>
            </ul>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Iniciar sesión", new_usuario_session_path, class: "nav-link" %>
          </li>
          <%# Sacamos el Registro porque ahora todo se gestiona desde AppNomina %>
          <!--li class="nav-item">
            <%#= link_to "Registrarse", new_usuario_registration_path, class: "nav-link" %>
          </li-->
        <% end %>
      </ul>
    </div>

  </div>
</nav>