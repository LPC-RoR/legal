<% if usuario_signed_in? and (not current_usuario.confirmed?) %>
	<%= render partial: 'usuarios/confirmations/info_registro' %>
<% else %>
	<%= render partial: 'layouts/partials/errr_msg', locals: {ttl: '¡Error de acceso!', dscrptn: 'Esta página no existe o requiere un usuario autenticado/verificado', hnt: 'Comuníquese con el administrador de la plataforma'} %>
<% end %>