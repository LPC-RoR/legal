<%# CONTEXTO: devise %>
<!DOCTYPE html>
<html>
  <% title = action_name == 'sign_in' ? 'Iniciar sesión | Laborsafe' : 'Autenticación de usuarios | Laborsafe' %>
  <%= render partial: 'layouts/partials/head_div', locals: {title: title || 'Laborsafe'} %>
  <body>

    <%# CONTEXTOS %>
    <%# public, pltfrm, invstgcns, srvcs %>

    <%= render partial: 'layouts/devise/navbar', locals: {logo_path: "logo/logo_130.png"} unless controller_name == 'servicios' %>

    <%= render partial: 'layouts/main' %>

    <%= render partial: 'layouts/partials/footer', locals: {logo_path: "logo/logo_60.png"} unless controller_name == 'servicios' %>

  </body>

</html>
