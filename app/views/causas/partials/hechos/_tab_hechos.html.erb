<%= render partial: 'causas/partials/hechos/titulo_hechos', locals: {objeto: objeto} %>

<%= render partial: '0p/tabla/tabla', locals: {c: 'temas'} %>

<% unless @coleccion['hechos'].empty? %>
	<div class="shadow p-3 mb-3 bg-white rounded">
		<% @coleccion['hechos'].each do |hecho| %>
				<%= render partial: 'hechos/list/objeto', locals: {objeto: hecho} %>
		<% end %>
	</div>
<% end %>
<hr>

<%= render partial: 'causas/partials/hechos/titulo_archivos', locals: {objeto: objeto} %>

<%= render partial: 'causas/partials/hechos/cllps_nuevo_archivo', locals: {objeto: objeto} %>
<%= render partial: 'causas/partials/hechos/cllps_sel_archivos', locals: {objeto: objeto} %>

<% unless @coleccion['app_archivos'].empty? %>
	<div class="shadow p-3 mb-3 bg-white rounded">
		<div class="row">
		<% @objeto.causa_archivos.order(:orden).each do |rel| %>
				<%= render partial: 'causa_archivos/list/lobjeto', locals: {objeto: rel} %>
		<% end %>
		</div>
	</div>
<% end %>