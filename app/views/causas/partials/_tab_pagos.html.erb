<div class="row">
	<div class="col-lg-4 col-xs-12">
		<%= render partial: 'capitan/layout/titulo_tabla', locals: {c: 'tar_uf_facturaciones', title: 'UF de cálculo', link_new: "/tar_uf_facturaciones/new?class_name=#{objeto.class.name}&objeto_id=#{@objeto.id}" } %>

		<%= render partial: '0p/tabla/tabla', locals: {c: 'tar_uf_facturaciones'} %>

	</div>
	<div class="col-lg-8">

		<%= render partial: 'capitan/layout/titulo_tabla', locals: {c: 'tar_pagos', title: 'Aprobación de pagos', link_new: nil } %>

		<% objeto.tar_tarifa.tar_pagos.order(:orden).each do |pago| %>

			<div class="shadow p-3 mb-3 bg-white rounded">
				<%= render partial: 'causas/partials/pago', locals: {objeto: objeto, pago: pago} %>
			</div>

		<% end %>

	</div>
</div>