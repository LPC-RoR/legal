<small class='text-info'>Detalle Cuantía</br>
<small class="text-muted">
<div class="row">
	<% objeto.valores_cuantia.each do |vc| %>
		<div class="col-8">
			<%= vc.detalle %>
		</div>
		<div class="col-4" align='right'>
			<b><%= "#{vc.moneda == 'Pesos' ? number_to_currency(vc.valor, locale: :en, precision: 0) : number_to_currency(vc.valor, unit: 'UF', precision: 2)}" %></b>
		</div>
	<% end %>
</div>
</small></small>