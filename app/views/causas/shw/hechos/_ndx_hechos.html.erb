<!-- Vista del PDF de la Demanda -->  
<% if objeto.demanda? and operacion? %>
	<iframe width="900" src=<%=objeto.demanda.archivo.url%>>
	    <p>Placeholder text; only shows up if the page DOESN'T render!</p>
	</iframe>
<% end %>

<%= render partial: '0p/general/clss_dir_c_ttl', locals: {clss: 'shw_ttl', dir: 'indx', source: 'hechos', ownr: objeto} %>

<!-- Despliegue de forms de Temas y hechos -->
<%= render partial: prtl_name('causas', 'shw/hechos', 'dsply'), locals: {objeto: objeto} %>

<%= render partial: '0p/tabla/tabla', locals: {c: 'temas'} %>

<% unless @coleccion['hechos'].empty? %>

	<!-- Si hay temas, los separamos de los hechos sin temas -->
	<% if objeto.temas.any? %><hr> <% end %>

	<% @coleccion['hechos'].each do |hecho| %>
			<%= render partial: 'csc/hechos/list/innr_bjt', locals: {objeto: hecho} %>
	<% end %>

<% end %>

<hr>

<%= render partial: '0p/general/clss_dir_c_ttl', locals: {clss: 'shw_ttl', dir: 'indx', source: 'rep_archivos', ownr: objeto} %>

<%= render partial: '0p/tabla/tabla', locals: {c: 'rep_archivos'} %>