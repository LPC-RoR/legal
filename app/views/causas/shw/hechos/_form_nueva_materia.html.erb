<div class="g_cllps">

	<h6>Nueva materia</h6>
	<%= form_for :nueva_materia, url: "/causas/#{objeto.id}/nueva_materia" do |form| %>

		<%= render partial: '0p/lbld_inpts/text', locals: { form: form, token: :tema, label: 'Tema' } %>
		
		<div class="text-end">
			<button type="submit" class="btn btn-primary">
			  <i class="bi bi-caret-right"></i>
			</button>
		</div>

	<% end  %>

</div>