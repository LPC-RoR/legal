<div class="g_cllps">
	<h6>Nuevo Hecho</h6>
	<%= form_for :nuevo_hecho, url: "/causas/#{objeto.id}/nuevo_hecho" do |form| %>

		<%= render partial: '0p/lbld_inpts/text_raw', locals: { form: form, token: :hecho, label: 'Hecho' } %>

		<%= render partial: '0p/lbld_inpts/text_area_raw', locals: { form: form, token: :descripcion, label: 'Descripción' } %>
		
		<div class="text-end">
			<button type="submit" class="btn btn-primary">
			  <i class="bi bi-caret-right"></i>
			</button>
		</div>

	<% end  %>
</div>