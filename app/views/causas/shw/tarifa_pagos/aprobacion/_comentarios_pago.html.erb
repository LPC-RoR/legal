<% if pago.valor.blank? %>
	<hr>

	<div class="ln_app">
		<div class="ln_nm">
			<b>Detalle aplicación de tarifa</b>
		</div>
	</div>
	<%= causa.facturaciones.first.tar_pago_id unless causa.facturaciones.empty? %>
	<%= causa.facturaciones.first.monto_pesos unless causa.facturaciones.empty? %>
	<%= causa.calculos.first.monto_pesos unless causa.calculos.empty? %>
	<%= causa.calculos.first.tar_pago_id unless causa.calculos.empty? %>
	<% unless pago.tar_comentarios.empty? %>
		<% pago.tar_comentarios.order(:orden).each do |comentario| %>

			<% if comentario.formula.blank? or calcula2(comentario.formula.strip, causa, pago) %>
				<%= render partial: 'causas/shw/tarifa_pagos/display_comentario', locals: {comentario: comentario, causa:causa, pago: pago} %>
			<% end  %>
			
		<% end %>
	<% end %>
		
<% end %>