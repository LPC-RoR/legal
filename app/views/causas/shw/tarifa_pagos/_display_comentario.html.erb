<div class="ln_dtll">
	<div class="ln_lft">
		<i class="bi bi-<%= comentario.despliegue.blank? ? 'lightbulb' : 'dot' %>"></i>
		<%= comentario.comentario %>
	</div>
	<div class="ln_rght">
		<% unless comentario.despliegue.blank? %>
			<%= s_moneda(comentario.moneda, calcula2(comentario.despliegue, causa, pago)) %>
		<% end %>
	</div>
</div>