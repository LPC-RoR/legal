<%= render partial: prtl_name('causas', 'indx', 'srch') %>

<div class="f_200pxlft_col">
  <div class="lft">
    <%= render partial: '0p/cmenu/cmenu', locals: { c: 'causas', tipo: tipo, estado: estado, path: path} %>
  </div>
  <div class="rght">
    <%= render partial: '0p/general/ttl_indx', locals: {cntrllr: 'causas', ttl: "Causas #{estado.blank? ? 'en cobranza' : cmenu_clss['causas'][:items][estado].downcase}", ownr: nil} %>

    <% unless @srch %>
      <%= render partial: '0p/tabla/tabla', locals: {c: 'causas'} %>
    <% else %>
      <% @cs_array.each do |fnd| %>
        <%= render partial: 'causas/list/objeto', locals: {objeto: fnd} %>
      <% end %>
    <% end %>
  </div>
</div>