<b>Último estado</b>

<div class="row px-2" style="font-size: 12px; line-height: 1.2;">
	<div class="col-6 mb-2">
		<%= objeto.estados.empty? ? 'Causa sin estados' : dma(objeto.estados.last.fecha) %>
	</div>
	<div class="col-6 mb-2 app_lnk text-end">
		<%= render partial: '0p/tbl/lnk_new', locals: {source: 'estados', blngs: objeto} %>
	</div>
	<% unless objeto.estados.empty? %>
		<div class="col-6">
			<%= objeto.estados.last.estado %>	
		</div>
		<div class="col-6 text-end app_lnk">
			<%= render partial: '0p/tbl/lnks_e', locals: {objeto: objeto.estados.last} %>
		</div>
	<% end %>
</div>

<% unless objeto.estados.empty? %>
	<div class="g_cllps">
		<%= render partial: '0p/tabla/links_e', locals: {objeto: objeto.estados.last} %>
		<%= objeto.estados.last.estado %>	
	</div>
<% end %>