<% dir ||= 'lst' %>
<%#= render partial: prtl_name(objt, 'list', 'stts'), locals: {objt: objt} %>
<b>
	<%= render layout: '0p/tltp_lnk', locals: {lnk: objt, tltp: 'Ver Causa'} do %>
			<%= objt.rit %>
	<% end %>
	<% if objt.estado_operativo == 'archivada' %>
		<i class="bi bi-archive-fill"></i>
	<% end %> : 
	<%= objt.causa %>
</b>

<!-- Warnings de la Causa -->
<% if objt.tar_tarifa.blank? || objt.tar_valor_cuantias.empty? || objt.medida_prejudicial || objt.nulidad_despido || objt.sin_cuantia %>
	<% clr_cone = objt.tar_tarifa.blank? || objt.tar_valor_cuantias.empty? ? 'danger' : 'primary' %>
	<i class="bi bi-dot"></i>
	<%= render layout: '0p/collapse/link', locals: {label: 'Warnings', objeto: objt, tooltip: 'Desplegar advertencias'} do %>
		<i class="bi bi-cone-striped text-<%=clr_cone%>"></i>
	<% end %>
<% end %>