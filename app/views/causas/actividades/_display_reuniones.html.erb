<p class="text-info">
	<small>
		Reuniones
		<%= link_to "/age_actividades/new?t=R&class_name=#{objeto.class.name}&objeto_id=#{objeto.id}" do %>
			<i class="bi bi-plus-circle"></i>
		<% end %>
	</small>
</p>


<% unless @coleccion['reunion-age_actividades'].empty?  %>
	<% @coleccion['reunion-age_actividades'].each do |reunion| %>
		<%= render partial: get_partial(reunion.class.name.tableize, 'list', "lobjeto") , locals: {objeto: reunion} %></br>
	<% end %>
<% end %>