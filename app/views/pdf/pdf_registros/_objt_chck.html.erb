<% unless code.nil? %>
	<% cone_clr ||= 'danger' %>
	<% pdf_rgstr = objeto.get_pdf_registro(code) %>
	<% pdf_archv = PdfArchivo.find_by(codigo: code) %>
	<br>
	<%# if objeto.info_oblgtr? %>
	<% if pdf_rgstr.present? %>
		<% if pdf_rgstr.audtd %>
			<i class="bi bi-clipboard-check text-primary"></i>
		<% else %>
			<i class="bi bi-check2-all text-primary"></i> 
		<% end %>
		<%= dma_hm(pdf_rgstr.created_at) %> 
		<%= pdf_archv.nombre %>
		<%= render partial: '0p/tbl/lnk_eliminar', locals: {objeto: pdf_rgstr} if admin? %>
	<% else %>
		<i class="bi bi-cone-striped text-<%= cone_clr %>"></i> 
		<%= pdf_archv.nombre %>
		<%= render layout: '0p/tltp_lnk', locals: {lnk:"/krn_reportes/audit_rprt?oid=#{@objeto.id}&rprt=#{code}"} do %>
			<i class="bi bi-clipboard-check"></i> Auditar!
		<% end %>
		<%= render layout: '0p/tltp_lnk', locals: {lnk:"/krn_reportes/generate_and_send_report?oid=#{@objeto.id}&rprt=#{code}", data: {turbo_method: :get, turbo_confirm: 'Confirmar envío de email'}} do %>
			<i class="bi bi-envelope-at"></i> Enviar!
		<% end %>
	<% end %>
<% end %>