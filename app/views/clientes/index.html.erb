<%#= render partial: '0p/tbl_ttl', locals: {source: 'clientes', ownr: (controller_name == 'clientes' ? @objeto : nil)} %>

<%= render partial: '0p/ttl_tbl', locals: {source: 'clientes'} %>
<%= render partial: '0p/tbl/tbl', locals: {source: 'clientes', typ: 'frame'} %>