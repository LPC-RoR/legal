<%= render layout: '0p/tltp_lnk', locals: {lnk: "/servicios/auditoria?oid=#{objeto.id}", tltp: 'Reporte Auditoría' } do %>
	<i class="bi bi-file-pdf"></i>
<% end %>

<%= render layout: '0p/tltp_lnk', locals: {lnk: "/cuentas/#{objeto.class.name.downcase[0]}_#{objeto.id}/dnncs", tltp: 'Cuenta del Cliente' } do %>
	<i class="bi bi-stack"></i>
<% end %>

<% if operacion? %>
	<i class="bi bi-dot"></i>
	<%= render partial: '0p/tbl/lnk_new', locals: {source: 'notas', ownr: objeto, gly: 'chat-left-text'} %>
	<%= render partial: '0p/tbl/lnks_e', locals: {objeto: objeto} %>
<% end %>