<div class="cl">
	<%= render partial: 'clientes/list/actions', locals: {objeto: objeto} %>
	<%= objeto.razon_social %>

	<% unless objeto.archivos_faltantes.empty? and objeto.archivos_vacios.empty? %>
		<%= render partial: '0p/general/nt', locals: {tipo: 'warning', nota: "Faltan #{objeto.archivos_faltantes.count + objeto.archivos_vacios.count} documentos controlados" } %>
	<% end %>

</div>