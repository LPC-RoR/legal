<%= render partial: '0p/ttl_clss', locals: {source: 'global_usuarios', ttl: 'Administración de roles de usuarios Addt', clss: :gray, dir: 'indx', hide_lnks: true} %>

<!-- global_usuarios/index -->
<% @usuarios.each do |u| %>
  <%= u.email %>
  <%= form_with model: u, url: global_usuario_path(u) do |f| %>
    <%= f.select :role, options_for_select(Usuario::ADDT_ROLES, u.role) %>
    <%= f.submit "Cambiar" %>
  <% end %>
<% end %>