<div class="d-flex justify-content-end gap-1">
	<%= render layout: '0p/mdl/link', locals: {cdg: objeto.codigo} do %>
		<i class="bi bi-question-circle"></i>
	<% end %>
	<% if prtl?(ownr, "hlp/#{objeto.codigo}", 'speech') %>
		<%= render layout: '0p/mdl/link', locals: {cdg: "#{objeto.codigo}_spch"} do %>
			<i class="bi bi-chat-right-dots"></i>
		<% end %>
	<% end %>
</div>