<% if prtl?('karin', "hlp/#{objeto.codigo}", 'hlp') %>

	<%= render layout: '0p/off_canvas/lnk', locals: {lbl: objeto.codigo} do %>
		<i class="bi bi-question-square"></i>
	<% end %>

	<%= render layout: '0p/off_canvas/dsply', locals: {lbl: objeto.codigo, ttl: objeto.tarea} do %>
	  <%= render prtl_name('karin', "hlp/#{objeto.codigo}", 'hlp'), locals: {objt: objeto} %>
	<% end %>

<% end %>