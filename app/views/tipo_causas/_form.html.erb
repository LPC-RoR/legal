<%= form_with(model: tipo_causa, local: true) do |form| %>
  <% if tipo_causa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tipo_causa.errors.count, "error") %> prohibited this tipo_causa from being saved:</h2>

      <ul>
      <% tipo_causa.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :tipo_causa %>
    <%= form.text_field :tipo_causa %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
