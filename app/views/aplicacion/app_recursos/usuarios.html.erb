<%= render partial: '0p/ttl_tbl', locals: {source: 'usu-app_recursos', lnks: 'ttl'} %>
<div class="shadow-sm p-3 mb-3 bg-white rounded">
	<% @coleccion['usuarios'].each do |usuario| %>
		<%= render partial: 'aplicacion/app_recursos/partials/usuario', locals: {objeto: usuario} %>
	<% end %>
</div>
<hr>

<% Empresa.all.each do |emp| %>
	<div><%=emp.razon_social%>:<%=emp.id%>-<%= emp.tenant.id %></div>
<% end %>