<% if finanzas? %>
	<div class="img_frm">
		<%= render partial: '0capitan/general/app_ttl', locals: {c: 'tar_facturas', title: "Facturacion", link: nil, tooltip: nil } %>
		<%= column_chart @facturacion, prefix: '$', thousands: ".", precision: 3 %>
	</div>
<% end %>
<% if finanzas? %>
	<% unless @coleccion['por_emitir-tar_facturas'].empty? %>
		<%= render partial: '0capitan/general/app_ttl', locals: {c: 'tar_facturas', title: "Facturas por emitir", link: nil, tooltip: nil } %>

		<%= render partial: '0p/tabla/tabla', locals: {c: 'por_emitir-tar_facturas'} %>
	<% end %>
	<% unless @coleccion['en_cobranza-tar_facturas'].empty? %>
		<%= render partial: '0capitan/general/app_ttl', locals: {c: 'tar_facturas', title: "Facturas en cobranza", link: nil, tooltip: nil } %>

		<%= render partial: '0p/tabla/tabla', locals: {c: 'en_cobranza-tar_facturas'} %>
	<% end %>
<% end %>