<div class="lyt_notice">
  Bienvenido(a) <%= nombre_perfil %>
</div>

<% if dog? %>
  <%= render layout: '0p/layout/tooltip_link', locals: {link: "/publicos/encuesta?sk=@#{@session_name}", tooltip: 'Deseo haecr la encuesta'} do %>
    Encuesta
  <% end %>
<% end %>

<div class="f_50lft_col">
  <div class="lft">
  </div>
  <div class="rght">
      <div class="ttl_app">
        <div class="ttl_ttl">
          <i class="bi bi-list-check"></i> 
          <%= @usuario.age_usuario %>
          <%= render layout: '0p/collapse/link', locals: {label: 'Personaliza', objeto: nil, tooltip: 'Personalizar usuario'} do %>
            <i class="bi bi-pencil-square"></i>
          <% end %>
        </div>
        <div class="ttl_lnks">
        </div>
      </div>
    <%= render layout: '0p/collapse/display', locals: {label: 'Personaliza', objeto: nil} do %>
      <%= render partial: 'actividades/age_actividades/partials/form_nombre' %>
    <% end %>
  </div>
</div>
