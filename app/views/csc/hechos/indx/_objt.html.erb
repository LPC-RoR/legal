Tabla de hechos
<i class="bi bi-dot"></i>
<%= render layout: '0p/layout/tooltip_link', locals: {link: "/causas/#{ownr.id}/set_flags?f=hreg", tooltip: "#{ownr.hechos_registrados ? 'Desbloquea hechos' : 'Bloquea hechos' }"} do  %>
	<%= render partial: '0p/general/gly', locals: {gly: "#{'check-' if ownr.hechos_registrados}square"} %>
<% end %>