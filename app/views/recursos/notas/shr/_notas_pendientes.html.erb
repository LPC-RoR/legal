<% objeto ||= ownr %>
<% unless objeto.notas.empty? %>
	<% objeto.notas.order(:created_at).each do |nota| %>
		<%= render partial: prtl('notas', 'lst', 'objeto'), locals: {objeto: nota} unless nota.realizado %>
	<% end %>
<% end %>