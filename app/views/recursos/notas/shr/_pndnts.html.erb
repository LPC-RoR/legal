<% prrdds 	= [nil, true].include?(prrdds) ? true : false %>
<% blue_dot = [nil, true].include?(blue_dot) ? true : false %>

<% unless ownr.notas.empty? %>
	<% ownr.notas.no_rlzds.crtd_at_asc.each do |nota| %>
		<%= render partial: prtl('notas', 'lst', 'objeto'), locals: {objeto: nota, prrdds: prrdds, blue_dot: blue_dot} %>
	<% end %>
<% end %>