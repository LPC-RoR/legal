<%# if prrdds or blue_dot %>
	<%= render partial: '0p/clr_point', locals: {clr: objt.prioridad} if prrdds %>
	<% if blue_dot %>
	<%= render partial: '0p/clr_point', locals: {clr: 'primary'} unless objt.realizado %>
	<% end %>
	<!--i class="bi bi-dot"></i-->
<%# end %>

<!--i class="bi bi-cone-striped text-<%#= objeto.realizado ? 'muted' : objeto.prioridad%>"></i-->
<%= dma_hm(objt.created_at) %>
<i class="bi bi-dot"></i>

<% if objt.ownr.class.name == 'Causa' and controller_name == 'publicos' %>
	<%= objt.ownr.causa %>
<% end %>

<%= render layout: '0p/collapse/link', locals: {label: 'Participantes', objeto: objt, tooltip: 'Participantes'} do %>
	<% if objt.age_usuarios.any? %>
		<i class="bi bi-person-check text-primary"></i>
	<% else %>
		<i class="bi bi-person-exclamation text-danger"></i>
	<% end %>
<% end %>

<%= render layout: '0p/collapse/link', locals: {label: 'Detalle', objeto: objt, tooltip: 'Detalle'} do %>
	<i class="bi bi-arrow-bar-down text-primary"></i>
<% end %>
