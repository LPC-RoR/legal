<% unless coleccion.empty? %>
	<div class="row">
		<% coleccion.each do |enlace| %>
			<%= render partial: prtl_name(enlace.class.name.tableize, 'list', "lobjeto") , locals: {objeto: enlace} %></br>
		<% end %>
	</div>
<% end %>