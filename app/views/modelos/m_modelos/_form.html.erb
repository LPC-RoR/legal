<%= form_with(model: m_modelo, local: true) do |form| %>
  <% if m_modelo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(m_modelo.errors.count, "error") %> prohibited this m_modelo from being saved:</h2>

      <ul>
      <% m_modelo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :m_modelo %>
    <%= form.text_field :m_modelo %>
  </div>

  <div class="field">
    <%= form.label :ownr_class %>
    <%= form.text_field :ownr_class %>
  </div>

  <div class="field">
    <%= form.label :ownr_id %>
    <%= form.number_field :ownr_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
