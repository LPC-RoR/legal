<%= form_with(model: m_reg_fact, local: true) do |form| %>
  <% if m_reg_fact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(m_reg_fact.errors.count, "error") %> prohibited this m_reg_fact from being saved:</h2>

      <ul>
      <% m_reg_fact.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :m_registro_id %>
    <%= form.number_field :m_registro_id %>
  </div>

  <div class="field">
    <%= form.label :tar_factura %>
    <%= form.number_field :tar_factura %>
  </div>

  <div class="field">
    <%= form.label :monto %>
    <%= form.text_field :monto %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
