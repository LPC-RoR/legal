<%= form_with(model: m_formato, local: true) do |form| %>
  <% if m_formato.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(m_formato.errors.count, "error") %> prohibited this m_formato from being saved:</h2>

      <ul>
      <% m_formato.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :m_formato %>
    <%= form.text_field :m_formato %>
  </div>

  <div class="field">
    <%= form.label :m_banco_id %>
    <%= form.number_field :m_banco_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
