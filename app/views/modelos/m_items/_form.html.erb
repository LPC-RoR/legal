<%= form_with(model: m_item, local: true) do |form| %>
  <% if m_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(m_item.errors.count, "error") %> prohibited this m_item from being saved:</h2>

      <ul>
      <% m_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :orden %>
    <%= form.number_field :orden %>
  </div>

  <div class="field">
    <%= form.label :m_item %>
    <%= form.text_field :m_item %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
