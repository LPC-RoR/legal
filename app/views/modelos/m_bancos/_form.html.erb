<%= form_with(model: m_banco, local: true) do |form| %>
  <% if m_banco.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(m_banco.errors.count, "error") %> prohibited this m_banco from being saved:</h2>

      <ul>
      <% m_banco.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :m_banco %>
    <%= form.text_field :m_banco %>
  </div>

  <div class="field">
    <%= form.label :m_modelo_id %>
    <%= form.number_field :m_modelo_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
