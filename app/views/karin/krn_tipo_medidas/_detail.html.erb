<%= render partial: '0p/frm_flds/text', locals: { form: form, token: :krn_tipo_medida, label: 'Medida tipo:' } %>

<div class='form-row'>
	<%= render partial: '0p/frm_flds/check_box', locals: { form: form, token: :denunciante, label: 'Denunciante?' } %>
	<%= render partial: '0p/frm_flds/check_box', locals: { form: form, token: :denunciado, label: 'Denunciado?' } %>
</div>

<%= render partial: '0p/frm_flds/radio', locals: { form: form, token: :tipo, label: 'Tipo', radio_list: ['Defecto', 'Opcional'] } %>

<%= render partial: '0p/form_fields/hidden', locals: { form: form, token: :cliente_id } %>