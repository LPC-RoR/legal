<% lst = ownr.krn_tramites.where(tipo: code) %>

<%= render partial: 'karin/kproc/trmt_code', locals: {code: code, mdl: mdl, ownr: ownr, lst: lst} %>

<% if lst.any? %>
	<div class="px-3">
		<%= render partial: 'karin/kproc/trmt_lst', locals: {code: code, mdl: mdl, ownr: ownr, lst: lst} %>
	</div>
<% end %>