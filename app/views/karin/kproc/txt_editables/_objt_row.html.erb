<% nombre = ClssTxtInvstgcns.nombre[code] %>

<div class="row gx-1">
	<div class="col-10">
		<i class="bi bi-file-text"></i>
		<% clr_cone = ClssTxtInvstgcns.optnl_code?(code) ? 'primary' : 'danger' %>
		<% if lst.none? %><small><i class="bi bi-cone-striped text-<%=clr_cone%>"></i></small><% end %>
		<%= nombre %>
	</div>
	<div class="col-2 text-end objt_lnk">
		<% unless lst.any? %>
			<%= render layout: '0p/tltp_lnk', locals: {lnk: "/txt_editables/new#{ownr_prms(ownr)}&cdg=#{code}", tltp: 'Subir PDF'} do %>
				<i class="bi bi-plus-circle"></i>
			<% end %>
		<% end %>
	</div>
</div>