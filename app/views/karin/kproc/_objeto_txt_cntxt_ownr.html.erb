<% lst = ownr.txt_editables.where(codigo: code) %>

<%= render partial: 'karin/kproc/objt_txt_code', locals: {code: code, mdl: mdl, ownr: ownr, lst: lst} %>

<% if lst.any? %>
	<div class="px-3">
		<%= render partial: 'karin/kproc/objt_txt_lst', locals: {code: code, mdl: mdl, ownr: ownr, lst: lst} %>
	</div>
<% end %>