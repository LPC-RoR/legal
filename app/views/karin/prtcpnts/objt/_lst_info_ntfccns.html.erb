<%# LISTA de envío : %>
<!-- Información y notificaciones -->

<%# hsh[código participante][acciones] %>
<%# each recorre las acciones que aplican a este participante %>
<% hsh[ownr.clss_id][:actns].each do |actn| %>

	<%# verifica que la accion no sea 'verificar_email', porque se resuelve por otro lado %>
	<% unless ClssPrcdmnt.list_excluded?(actn) %>

		<%= render partial: 'karin/prtcpnts/objt/actn_objt', locals: {audit: ownr.dnnc.auditoria, hsh: @acts_hsh, mdl: ClssPrcdmnt, ownr: ownr, code: actn} %>
		
	<% end %>

<% end %>