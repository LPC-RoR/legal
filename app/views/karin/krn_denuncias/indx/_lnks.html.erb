<!-- cnfgrcn_ok? vsalida krn_investigadores, krn_empresas_externas, app_nominas y app_contactos -->
<% if @objeto.cnfgrcn_ok? %>
	<% autorize = current_usuario.operacion? or current_usuario.recepcion? %>
	<!-- @licencia_activa valida segín modelo Licencia -->
	<% if (@licencia_activa and autorize) or current_usuario.dog? %>
		<%= render partial: '0p/tabla/lnk_new', locals: {lnk: "/#{cntrllr('krn_denuncias')}/new#{ownr_prms(@objeto) unless @objeto.blank?}", tltp: "Agrega #{to_name(cntrllr('krn_denuncias'))}"} %>
	<% end %>
<% end %>