<%= render partial: '0p/ttl_shw', locals: {objt: @objeto} %>

<!-- ETAPAS REALIZADAS -->
<%= render partial: 'karin/prtls/etps_rlzds', locals: {ownr: @objeto} %>

<!-- Registros acumulados de la denuncia -->
<%= render partial: '0p/proc/dnnc_rgstrs', locals: {trsh_cndtn: false, objeto: @objeto} %>

<!-- Registros acumulados de la denuncia -->
<%= render partial: 'karin/prtls/nts', locals: {objeto: @objeto} %>

<!-- ETAPA y TAREA activa -->
<%= render partial: 'karin/prtls/etp_tar', locals: {ownr: @objeto} %>

<!-- Despliegue de Denunciantes y Denunciados -->
<%= render partial: 'karin/prtls/prtcpnts', locals: {ownr: @objeto} %>

<!-- Archivo de la denuncia -->
<%= render partial: 'karin/prtls/dcs', locals: {ownr: @objeto} %>

<!-- Resumen de las declaraciones -->
<%= render partial: 'karin/prtls/dclrcns', locals: {ownr: @objeto} %>

<%= @proc[:etp_last].codigo %>
<%= @proc[:tar_last].codigo unless @proc[:tar_last].blank? %>
<%= @objeto.fechas_invstgcn? and @objeto.chck_dvlcn? %>
<%= @objeto.frms_ingrs? %>
-
<%= @objeto.rcp_externa? and @objeto.krn_empresa_externa_id.blank? %>
<%= @objeto.via_declaracion == KrnDenuncia::VIAS_DENUNCIA[0] and @objeto.tipo_declaracion.blank? %>
<%= @objeto.presentado_por == KrnDenuncia::TIPOS_DENUNCIANTE[1] and @objeto.representante.blank? %>
-
<%= @objeto.proc_externa? %>
<%= @objeto.proc_tipo? %>

<%# if dog? %>
	<%#= "rep_archivos" if @objeto.rep_archivos.any? %>
	<%#= "valores" if @objeto.valores.any? %>
	<%#= "ctr_registros" if @objeto.ctr_registros.any? %>
	<%#= "krn_denunciantes" if @objeto.denunciantes? %>
	<%#= "krn_denunciados" if @objeto.denunciados? %>
	<%#= "krn_derivaciones" if @objeto.derivaciones? %>
	<%#= "krn_declaraciones" if @objeto.declaraciones? %>
	<%#= "krn_inv_denuncias" if @objeto.krn_inv_denuncias.any? %>
<%# end %>