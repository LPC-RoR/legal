<%= render partial: '0p/proc/ln', locals: {glosa: "DOCUMENTACIÓN OBLIGATORIA", bld: true} %>

<% @fls_actv.each do |dc| %>
	<% if fl_cndtn?(ownr, dc.codigo) or ownr.fl?(dc.codigo) %>
		<% if dc.multiple == true %>
			<%= render partial: prtl_name(ownr, 'shw/proceso/reporte', 'dcs'), locals: {dc: dc, fls: ownr.fls(dc), ownr: ownr} %>
		<% else %>
			<%= render partial: prtl_name(ownr, 'shw/proceso/reporte', 'dc'), locals: {dc: dc, fl: ownr.fl(dc), ownr: ownr} %>
		<% end %>
	<% end %>
<% end %>