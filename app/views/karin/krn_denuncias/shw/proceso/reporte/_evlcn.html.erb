<% if objeto.evlcn_ok %>
	<%= render partial: '0p/proc/ln', locals: {glosa: "Análisis: la denuncia no presenta inconsistencias."} %>
<% else %>
	<% if objeto.evlcn_incnsstnt %>
		<%= render partial: '0p/proc/ln', locals: {glosa: "Análisis: la denuncia presenta inconsistencias."} %>
	<% end %>
	<% if objeto.fecha_hora_corregida? %>
		<%= render partial: '0p/proc/ln', locals: {glosa: "Plazo para presentar denuncia corregida.", valor: dma(objeto.fecha_hora_corregida), align: 'right', bld: true} %>
	<% end %>
<% end %>