<% unless @etp_last.blank? or @tar_last.blank? %>
	<% if prtl?(ownr, "proc/#{@tar_last.codigo}", 'nts') or (@objt[:tar_cntrl][@tar_last.codigo][:frms] and @tar_last.dsply?(@objeto)) or prtl?(ownr, "proc/#{@tar_last.codigo}", 'chlds') %>

		<% if @objt[:tar_cntrl][@tar_last.codigo][:frms] and @tar_last.dsply?(@objeto) %>
			<%= render partial: '0p/prtls/dsply', locals: {objt: @objeto, dir: "proc/#{@tar_last.codigo}", prtl: 'frm'} %>
		<% end %>

	<% end %>
<% end %>