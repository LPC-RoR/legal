<% if tab_proceso? %>
	<%= render layout: 'karin/krn_denuncias/shw/proceso/tsk_tmplt', locals: {ttl: "Evidencias de la aplicación de medidas correctivas y sanciones & cierre del procedimiento", gly: 'lightbulb'} do %>

		<% if @objeto.tiene_mdds_sncns? %>
			<p class="mt-2 px-2"><i class="bi bi-calendar-event text-primary"></i> La fecha de la última evidencia subida a la plataforma tiene fecha <%=dma(@objeto.fecha_ultima_evidencia)%>.</p>
			<%= render partial: '0p/proc/etp_frm', locals: {etp: @kproc.etapa, objeto: @objeto, code: 'fecha_cierre', plz_fld: true} %>
		<% else %>
			<p class="mt-2 px-2"><i class="bi bi-cone-striped text-danger"></i> La plataforma no registra, hasta la fecha, evidencias que acrediten la aplicación de las medidas correctivas o sanciones correspondientes.</p>
		<% end %>

	<% end %>
<% end %>