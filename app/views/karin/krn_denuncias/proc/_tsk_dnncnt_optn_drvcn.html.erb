<% if tab_proceso? %>
	<%= render layout: 'karin/krn_denuncias/shw/proceso/tsk_tmplt', locals: {ttl: 'La persona denunciante debe manifestar su preferencia respecto a que entidad realizará la investigación', gly: 'lightbulb'} do %>

		<% if @objeto.tiene_art4_1? %>
			<%= render partial: 'karin/krn_derivaciones/lnk_drvcn', locals: {ownr: @objeto, color: 'success', codigo: 'drvcn_art4_1'} %>
		<% else %>
			<%= render partial: 'karin/krn_derivaciones/lnk_drvcn', locals: {ownr: @objeto, color: 'success', codigo: 'drvcn_dnncnt'} %>

			<div class="fw-bold">
				<%= render partial: '0p/swtch', locals: {objeto: @objeto, tkn: :dnncnt_investigacion_local, prmpt: 'La persona denunciante expresa su voluntad de que la investigación sea llevada a cabo por la empresa.', glys: ['arrow-counterclockwise', 'cursor'], stts: ['preferencia eliminada', 'empresa']} %>
			</div>
		<% end %>

		<div class="my-2 text-secondary" style="font-size: 0.7rem;">
			<i class="bi bi-cone-striped"></i> Esta tarea es la última que requiere definiciones con la persona denunciante.
		</div>
	<% end %>
<% end %>
