<% clccn = ownr.krn_declaraciones.order(:fecha) %>

<% if ownr.krn_declaraciones.any? %>
	<%= render partial: '0t/tbl', locals: {clccn: clccn, typ: 'ln', hide_ee: true} %>
<% else %>
	<i class="bi bi-cone-striped text-danger"></i> Agenda vacía 
<% end %>