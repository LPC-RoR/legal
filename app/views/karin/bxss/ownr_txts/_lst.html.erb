<div class="p-2">
	<%# PDFs GENERADOS DESDE SERVICIOS CON CONTEXTO %>
	<% txt_codes = ClssTxtInvstgcns.dsply_codes_for(ownr) %>

	<% txt_codes.each do |code| %>
		<%= render partial: 'karin/kproc/objeto_txt_cntxt_ownr', locals: {code: code, mdl: ClssTxtInvstgcns, ownr: ownr} %>
	<% end %>
</div>