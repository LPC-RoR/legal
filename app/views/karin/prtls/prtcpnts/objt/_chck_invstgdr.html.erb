<% if @objeto['investigadores?'] %>
	<br>
	<% if objeto.invstgdr_sent? %>
		<i class="bi bi-check2-all text-primary"></i> Se envió notificación del investigador asignado.
	<% else %>
		<i class="bi bi-cone-striped text-danger"></i> Se debe enviar notificación del investigador asignado por correo electrónico.
		<%= render layout: '0p/tltp_lnk', locals: {lnk:"/krn_reportes/generate_and_send_report?oid=#{@objeto.id}&rprt=invstgdr", data: {turbo_method: :get, turbo_confirm: 'Confirmar envío de email'}} do %>
			<i class="bi bi-envelope-at"></i> Enviar!
		<% end %>
	<% end %>
<% end %>