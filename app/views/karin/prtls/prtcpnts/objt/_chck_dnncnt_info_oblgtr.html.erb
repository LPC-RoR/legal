<% if objeto.class.name == 'KrnDenunciante' %>
	<br>
	<% if objeto.info_oblgtr? %>
		<i class="bi bi-check2-all text-primary"></i> Se envió la información obligatoria para personas denunciantes por correo electrónico.
	<% else %>
		<i class="bi bi-cone-striped text-<%= @objt['verbal?'] ? 'primary' : 'danger' %>"></i> Se debe enviar la información obligatoria para personas denunciantes por correo electrónico.
		<%= render layout: '0p/tltp_lnk', locals: {lnk:"/krn_reportes/generate_and_send_report?oid=#{@objeto.id}&rprt=dnncnt_info_oblgtr", data: {turbo_method: :get, turbo_confirm: 'Confirmar envío de email'}} do %>
			<i class="bi bi-envelope-at"></i> Enviar!
		<% end %>
	<% end %>
<% end %>