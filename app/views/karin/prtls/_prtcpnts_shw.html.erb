<%= render partial: '0p/ttl_shw', locals: {objt: objeto} %>
<div class="mt-1 bg-white shadow border rounded p-2">

	<!-- Activar si requieren registros -->
	<%= render partial: '0p/proc/dnnc_rgstrs', locals: {objeto: objeto} %>

	<%= render partial: 'karin/prtls/prtcpnts_dclrcns', locals: {objeto: objeto} %>
	<%= render partial: 'karin/prtls/prtcpnts_tstgs', locals: {objeto: objeto} %>

</div>
<!-- NOTAS -->
<%= render partial: 'karin/prtls/nts', locals: {objeto: objeto} %>

<%= render partial: 'karin/prtls/dcs', locals: {ownr: objeto} %>