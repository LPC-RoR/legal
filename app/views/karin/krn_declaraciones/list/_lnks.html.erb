<% if objeto.pdf_registros.empty? %>
	<%= render layout: '0p/tltp_lnk', locals: {lnk: "/krn_reportes/generate_and_send_report?oid=#{objeto.id}&rprt=dclrcn"} do %>
		<i class="bi bi-envelope-at"></i>
	<% end %>
<% else %>
	<i class="bi bi-check2-all"></i>
<% end %>

<% unless objeto.rlzd %>
	<small><%= render partial: '0p/tbl/lnks_e', locals: {objeto: objeto} %></small>
<% end %>