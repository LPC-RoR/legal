<% if objeto.invstgdr_objtd? or objeto.invstgdr_elmnd? %>
	<% if objeto.krn_denuncia.krn_investigadores.count == 1 %>
		<i class="bi bi-bug"></i>
	<% else %>
		<%= render partial: '0p/tltp_lnk', locals: {lnk: "/krn_declaraciones/#{objeto.id}/migrar" , tltp: 'Traspasar agenda'} do %>
			<i class="bi bi-pencil-square"></i>
		<% end %>
	<% end %>
<% end %>

<b><%= dma_hm(objeto.fecha) %></b> : <%= objeto.ownr.nombre %>
