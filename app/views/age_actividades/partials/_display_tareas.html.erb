<p class="text-info">
	<small>
		Tareas
		<%= link_to "/age_actividades/new?t=T&class_name=#{objeto.class.name}&objeto_id=#{objeto.id}" do %>
			<i class="bi bi-plus-circle"></i>
		<% end %>
	</small>
</p>


<% unless coleccion.empty?  %>
	<% coleccion.each do |tarea| %>
		<%= render partial: get_partial(tarea.class.name.tableize, 'list', "lobjeto") , locals: {objeto: tarea} %></br>
	<% end %>
<% end %>