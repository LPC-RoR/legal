<i class="bi bi-person-lines-fill text-info"></i>
<!--small><%#= objeto.filo_orden.filo_orden unless objeto.filo_orden.blank? %></small> |  -->
<small>
	<%= link_to objeto.org_cargo, "/servicios/cargos?cid=#{objeto.id}" %> 
	( <%= objeto.dotacion.blank? ? '-' : objeto.dotacion %> ) | 
	<%= link_to "/org_empleados/new?cid=#{objeto.id}", title: 'Nuevo empleado', 'data-toggle' => 'tooltip', 'data-placement' => 'right' do %>
		<i class="bi bi-person-plus"></i>
	<% end %>

	<%= render partial: '0p/tbl/lnks_e', locals: {objeto: objeto} if admin? %>
</small>
</br>
