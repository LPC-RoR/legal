<i class="bi bi-person text-info"></i>
<!--small><%#= objeto.filo_orden.filo_orden unless objeto.filo_orden.blank? %></small> |  -->
<small>
	<%= link_to objeto.empleado, "/servicios/empleados?eid=#{objeto.id}" %> | <%= objeto.d_rut %> 
	<% unless objeto.org_sucursal.blank? %>
		<i class="bi bi-pin-map"></i>
	<% end %> 

	<% if admin? %>
		<%= render partial: '0p/tabla/links_e', locals: {objeto: objeto} %>
	<% end %>
</small>
</br>
