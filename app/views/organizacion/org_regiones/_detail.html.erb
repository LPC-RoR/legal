<%= render partial: '0p/form_fields/label', locals: { form: form, token: :region_id, label: 'Region' } %>
<%= render partial: '0p/form_fields/select_id', locals: { form: form, token: :region_id, colection: Region.all.order(:orden).collect { |reg| [reg.region, reg.id] }, display_field: 'region', prompt: 'Región' } %>

<%= render partial: '0p/form_fields/hidden', locals: { form: form, token: :cliente_id } %>
