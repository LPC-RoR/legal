<%= render partial: '0p/lbld_inpts/select_id', locals: { form: form, token: :region_id, colection: Region.all.order(:orden).collect { |reg| [reg.region, reg.id] }, display_field: 'region', prompt: 'Región', label: 'Region' } %>

<%= render partial: '0p/lbld_inpts/hidden', locals: { form: form, token: :cliente_id } %>
