<h4><%= render partial: '0p/general/icon_text', locals: {gly: 'file-text', text: "Reporte #{@objeto.nombre_padre} (#{@objeto.periodo})", clr: app_color[:app]} %></h4>
<h5><%= "Total Reportem : #{@objeto.moneda_reporte == 'Pesos' ? '$' : 'UF'} #{@objeto.monto_reporte}" %></h5>
<!-- BOTON para ir a OWNER -->
<%= render partial: '0p/general/btn_proceso', locals: {texto: "#{@objeto.owner.class.name}", link: "/#{@objeto.owner.class.name.downcase.pluralize}/#{@objeto.owner.id}?html_options[tab]=Reportes", gly: 'arrow-left', clr: color(@objeto.class.name.tableize), delete: false} %>

<%= render partial: '0p/tabla/solo_tabla', locals: {c: 'registros', a: 'index'} %>
