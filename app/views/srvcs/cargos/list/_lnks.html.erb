<% if objeto.detalle? %>
	<%= render layout: '0p/collapse/link', locals: {label: 'Info', objeto: objeto, tooltip: 'Ver información adicional'} do %>
		<i class="bi bi-arrow-bar-down"></i>
	<% end %>
<i class="bi bi-dot"></i>
<% end %>
<!--small>
	<%#= render partial: '0p/swtch', locals: {objeto: objeto, tkn: 'urgente', stts: ['normal', 'urgente'], glys: ['circle-fill', 'circle-fill'], clr: 'danger'} %>
	<%#= render partial: '0p/swtch', locals: {objeto: objeto, tkn: 'pendiente', stts: ['pendiente oficina', 'pendiente cliente'], glys: ['circle-fill', 'circle-fill'], clr: 'primary'} %>
</small-->
<%= render partial: 'clientes/shr/lnk_cl', locals: {objeto: objeto} %>
<i class="bi bi-dot"></i>
<% if operacion? %>
	<%= render partial: '0p/tbl/lnk_new', locals: {source: 'notas', ownr: objeto, gly: 'chat-left-text'} %>
	<%= render partial: '0p/tbl/lnks_e', locals: {objeto: objeto} if check_crud(objeto) %>
<% end %>