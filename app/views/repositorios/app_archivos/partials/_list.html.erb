<% unless coleccion.empty? %>
	<div class="row">
		<% coleccion.each do |archivo| %>
			<%= render partial: partial_name(archivo.class.name.tableize, 'list', "lobjeto") , locals: {objeto: archivo} %></br>
		<% end %>
	</div>
<% end %>