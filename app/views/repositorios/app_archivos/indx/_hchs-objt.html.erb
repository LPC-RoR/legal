Archivos requeridos
<i class="bi bi-dot"></i>
<%= render layout: '0p/layout/tooltip_link', locals: {link: "/causas/#{ownr.id}/set_flags?f=areg", tooltip: "#{ownr.archivos_registrados ? 'Desbloquea archivos' : 'Bloquea archivos' }"} do  %>
	<%= render partial: '0p/general/gly', locals: {gly: "#{'check-' if ownr.archivos_registrados}square"} %>
<% end %>