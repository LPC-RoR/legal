Archivos requeridos
<i class="bi bi-dot"></i>
<%= render layout: '0p/tltp_lnk', locals: {lnk: "/causas/#{ownr.id}/set_flags?f=areg", tltp: "#{ownr.archivos_registrados ? 'Desbloquea archivos' : 'Bloquea archivos' }"} do  %>
	<%= render partial: '0p/general/gly', locals: {gly: "#{'check-' if ownr.archivos_registrados}square"} %>
<% end %>