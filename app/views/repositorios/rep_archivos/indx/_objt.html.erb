<% if krn_routes? %>
	Documentación obligatoria
<% else %>
	Archivos requeridos
<% end %>
<% if ownr.class.name == 'Causa' %>
	<i class="bi bi-dot"></i>
	<%= render partial: '0p/swtch', locals: {objeto: ownr, tkn: 'archivos_registrados', stts: ['Desbloqueado', 'Bloqueado']} %>
<% end %>