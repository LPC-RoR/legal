<% unless coleccion.empty? %>
	<% coleccion.each do |archivo| %>
		<% if seguridad(archivo.visible_para) %>
			<%= render partial: prtl_name(archivo.class.name.tableize, 'list', "bjt") , locals: {objeto: archivo} %>
		<% end %>
	<% end %>
<% end %>