<% cdgs  = ClssPrcdmnt.archivos_que_aplican(ownr) %>

<% if cdgs %>

	<% cdgs.each do |code| %>

		<%= render partial: prtl('act_archivos', 'objt', 'objeto_cntrld'), locals: {audit: ownr.dnnc.auditoria, mdl: ClssPrcdmnt, ownr: ownr, code: code, dsply_lnks: dsply_lnks} %>

	<% end %>

<% end %>