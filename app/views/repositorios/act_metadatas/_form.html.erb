<%= form_with(model: act_metadata) do |form| %>
  <% if act_metadata.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(act_metadata.errors.count, "error") %> prohibited this act_metadata from being saved:</h2>

      <ul>
        <% act_metadata.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :act_metadata, style: "display: block" %>
    <%= form.text_field :act_metadata %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
