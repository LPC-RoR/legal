<%= form_with(model: app_escaneo, local: true) do |form| %>
  <% if app_escaneo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(app_escaneo.errors.count, "error") %> prohibited this app_escaneo from being saved:</h2>

      <ul>
      <% app_escaneo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :ownr_class %>
    <%= form.text_field :ownr_class %>
  </div>

  <div class="field">
    <%= form.label :ownr_id %>
    <%= form.number_field :ownr_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
