<small>
	<div class="row">
		<div class="col-10">
			<i class="bi bi-key"></i>
			<%= objeto.app_sigla %><i class="bi bi-dot"></i><%= objeto.app_nombre %>
			<% unless objeto.dog_perfil.blank? %>
				<i class="bi bi-arrow-right"></i>
				<%= objeto.dog_perfil.nombre_perfil %>
			<% end %>
		</div>
		<div class="col-2" align='right'>
			<%= render partial: '0p/tabla/link_editar', locals: {objeto: objeto} %>
		</div>
	</div>
</small>