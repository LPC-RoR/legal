<i class="bi bi-person-square text-<%=objeto.app_perfil.blank? ? 'danger' : 'info' %>"></i> 
<%= objeto.nombre %>
<small>
	<% unless objeto.tipo.blank? %>
		<i class="bi bi-dot"></i>
		<i class="bi bi-bookmark"></i>
		<%= objeto.tipo %>
	<% end %>
<!--/br-->
	<i class="bi bi-arrow-right"></i>
	<i class="bi bi-person<%='-circle' if objeto.tipo == 'admin'%>"></i>
	<%= objeto.email %> 
</small>