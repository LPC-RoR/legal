<%= form_with url: "/#{objeto.class.name.tableize}/#{objeto.id}/fll_cltn_id?k=#{key}", method: :post, local: true do |form| %>
  <div class="get_dttm">
    <div class="f_search">
      <div class="lft">
          <%= render partial: '0p/frm_flds/select_id', locals: {form: form, token: :cltn_id, colection: coleccion, label: nil, prompt: prmpt} %>
      </div>
      <div class="right" align='right'>
        <button type="submit" class="btn btn-info">
          <i class="bi bi-arrow-right"></i>
        </button>
      </div>
    </div>
  </div>
<% end %>