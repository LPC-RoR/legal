<div class="<%=clss%>">
	<div class="ttl">
		<i class="bi bi-<%=actvbx_gly[accn]%>"></i> 
		<% if prtl?(objeto.class.name.tableize, 'code/prmpt', code) %>
			<b><%= render partial: prtl_name(objeto.class.name.tableize, 'code/prmpt', code), locals: {objeto: objeto} %></b>
		<% end %>
	</div>
</div>