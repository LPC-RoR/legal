<%= donut_chart(hsh,
  class: 'box',
  options: {
    legend: {
      bottom: '0'
    },
    radius: ['40%', '70%'], # Mover aquí
    emphasis: {
      itemStyle: {
        shadowBlur: 10,
        shadowOffsetX: 0,
        shadowColor: 'rgba(0, 0, 0, 0.5)'
      }
    }
  }
) %>