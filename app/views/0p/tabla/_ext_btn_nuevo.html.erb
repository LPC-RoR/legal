<%= link_to  "/#{c}/new?class_name=#{(o.blank? ? 'nil' : o.class.name)}&objeto_id=#{(o.blank? ? 'nil' : o.id)}", class: "btn btn-outline-#{color(c)} btn-sm" do %>
	<% unless gly.blank? %>
		<i class=<%="bi-#{gly}"%>></i>
	<% end %>
    <%= cristiano(c, 'controller', 'singular') unless ['app_directorios', 'app_documentos'].include?(c) %>
<% end %>
