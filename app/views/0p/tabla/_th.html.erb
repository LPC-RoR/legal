<tr>
	<% c.classify.constantize::TABLA_FIELDS.each do |field| %>
		<% if sortable?(c, get_field_name(field)) %>
			<td><small><i><%= sortable corrige_campo(get_field_name(field).delete_prefix('d_')) %></i><small></td>
		<% else %>
			<td><i><small><%= corrige_campo(get_field_name(field).delete_prefix('d_')) %></i></small></td>
		<% end %>
	<% end %>
    <td colspan="1"></td>
</tr>
