<tr style="font-size: 15px;">
	<td class="text-<%=fls.empty? ? 'muted' : 'dark'%> app_lnk" style="padding-left: 1rem;">
		<b>
			<i class="bi bi-list-task"></i>
			<%= dc.rep_doc_controlado %>
		</b>
	</td>
	<td class="app_lnk" style="text-align: right; padding-right: 1rem;">
		<%= render partial: '0p/tabla/lnk_new', locals: {lnk: "/rep_archivos/new#{ownr_prms(ownr)}&dcid=#{dc.id}", tltp: " Agregar #{dc.rep_doc_controlado}" } %>
	</td>
</tr>

<% fls.each do |fl| %>
	<%= render partial: '0p/proc/dc', locals: {dc: dc, fl: fl, ownr: ownr, pddn: 3} %>
<% end %>