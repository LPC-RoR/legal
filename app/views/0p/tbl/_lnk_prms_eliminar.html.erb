<!-- NO usa has_child? -->
<% return if objt.nil? %>
<%= link_to url_params(objt, local_assigns[:prms] || {}, :destroy),
            method: :delete,
            data: {
              turbo_method: :delete,
              turbo_confirm: "¿Estás seguro?",
              bs_toggle:    'tooltip',
              bs_placement: 'right',
              bs_title:     'Eliminar',
              bs_custom_class: 'custom-tooltip'
            },
            title: 'Eliminar' do %>
  <i class="bi bi-trash"></i>
<% end %>