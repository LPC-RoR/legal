<%= link_to "/#{cntrllr(source)}/new#{ownr_prms(ownr) unless ownr.blank?}", remote: true, data: { bs_toggle: 'modal', bs_target: '#formModal', data_action: "new" } do %>
  <i class="bi bi-plus-circle"></i>
<% end %>
