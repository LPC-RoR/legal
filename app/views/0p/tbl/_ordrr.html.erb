<% unless objeto.orden == 1 %>
	<%= render layout: '0p/tltp_lnk', locals: {lnk: "/#{objeto.class.name.tableize}/#{objeto.id}/arriba", tltp: 'Mover arriba', data: {turbo_method: :post}} do %>
		<i class="bi bi-arrow-up-short"></i>
	<% end %>
<% end %>
<% unless objeto.orden == objeto.n_list %>
	<%= render layout: '0p/tltp_lnk', locals: {lnk: "/#{objeto.class.name.tableize}/#{objeto.id}/abajo", tltp: 'Mover abajo', data: {turbo_method: :post}} do %>
		<i class="bi bi-arrow-down-short"></i>
	<% end %>
<% end %>