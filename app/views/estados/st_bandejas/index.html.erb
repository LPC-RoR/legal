<% if nomina_activa? %>
	<% unless @m.blank? or @e.blank? %>
		<%= render partial: '0p/general/gly_title_size', locals: {gly: controller_icon[@m.tableize], title: "#{m_to_name(@m)} : #{@e}", size: 5} %>

		<%= render partial: '0p/tabla/tabla', locals: {c: @m.tableize} %>
	<% else %>
		Sin información para desplegar
	<% end %>
<% end %>
