<%= render partial: '0p/ttl_shw', locals: {objt: @objeto} %>

<% unless @objeto.operable? or ( not admin? ) %>
	<%= render partial: prtl_name('cuentas', 'prtls', 'init_nomina') %>
<% else %>
<% end %>
<%= render partial: '0p/tbl_ttl', locals: {source: 'app_nominas', ownr: @objeto} if admin? %>

<% if dog? %>
	<%= render partial: '0p/tbl_ttl', locals: {source: 'pro_dtll_ventas', ownr: @objeto} %>
<% end %>