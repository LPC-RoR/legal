<%= render partial: 'cuentas/prtls/menu_icons' %>

<%= render partial: 'cuentas/indx/ttl', locals: {source: source, ownr: @objeto, fs: 3, brdr_clr: 'dark-subtle'} %>

<% unless @objeto.operable? %>
	<%= render partial: prtl_name('cuentas', 'prtls/init', init_prtl) %>
<% else %>
	<%= render partial: 'cuentas/indx/tbl', locals: {source: source, crud: 'lnks'} %>
<% end %>