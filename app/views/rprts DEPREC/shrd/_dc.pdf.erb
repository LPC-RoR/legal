<% pddng ||= nil %>
<% gls = "#{'(pendiente)' if fl.blank?} #{dma(fl.fecha) if fl.present? and fl.control_fecha} #{fl.blank? ? dc.rep_doc_controlado : (fl.nombre.blank? ? fl.rep_archivo : fl.nombre)}" %>
<%= render partial: 'rprts/shrd/tr', locals: {glosa: gls, gly: 'file-earmark-arrow-up', pddng: pddng} %>